# NOTES

`init.el` in `preload` probably needs to be taken out of search for the first boot-up of Emacs Prelude.

## PACKAGES

Packages to install manually (probably before `personal/preload/init.el` established):

- `cider`
- `rainbow-delimiters`

## TODO
